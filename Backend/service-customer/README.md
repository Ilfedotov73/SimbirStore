## Запуск
```
docker-compose up --build -d
```
- Сервис будет доступен по адресу: http://localhost:8080/
- Swagger UI будет доступен по адресу: http://localhost:8080/docs/index.html

## Запуск тестов
```
make test
```